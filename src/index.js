import React from 'react';
import ReactDOM from 'react-dom';
import Anaedor from './components/Anaedor';

ReactDOM.render(
    <React.StrictMode>
        <Anaedor />
    </React.StrictMode>,
    document.getElementById('root')
);